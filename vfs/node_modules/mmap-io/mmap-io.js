// Generated by LiveScript 1.3.1
var mmapIo, privateSync;
mmapIo = require("./build/Release/mmap-io.node");
privateSync = mmapIo.sync_lib_private__;
delete mmapIo.sync_lib_private__;
mmapIo.sync = function(buf, offset, size, blockingSync, invalidatePages){
  switch (typeof offset) {
  case "boolean":
    blockingSync = offset;
    invalidatePages = size != null ? size : false;
    offset = 0;
    size = buf.length;
    break;
  case "number":
    blockingSync == null && (blockingSync = false);
    invalidatePages == null && (invalidatePages = false);
    break;
  default:
    offset = 0;
    size = buf.length;
    blockingSync = false;
    invalidatePages = false;
  }
  privateSync(buf, offset, size, blockingSync, invalidatePages);
};
module.exports = mmapIo;