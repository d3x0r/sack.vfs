

// open a socket/http service
// need a UI shell
//  - login client (UserDb init)
//  - check access tokens



processMessage(ws,msg) {
    switch( msg.op ) {
    case "subscribe":
        break;
    case "post":
        break;
    case "makeForum":
        break;

    default:
        break;
    }
}
